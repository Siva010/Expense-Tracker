  return (
    <div className={`h-[61px] border-b sticky top-0 z-30 ${
      isDark 
        ? "bg-gray-900 border-gray-700" 
        : "bg-white border-gray-400"
    }`}>
      {/* Top navbar content */}
    </div>
  ); 